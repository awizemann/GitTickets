# Tasks

> Repo-resident task board managed by Memophant and Claude sessions. Move items between
> sections as work progresses; checklist state mirrors the section.

## Ideas


## Todo

- [ ] **iOS Sim XCTest Keychain entitlement failures**: Discovered during PR 14 iOS Sim test run — 26 tests across `TokenStoreTests`, `KeychainTests`, `DeviceIdentityTests` fail with `errSecMissingEntitlement (-34018)` when run via `xcodebuild test` on iOS Sim (real iOS devices and `swift test` on macOS both work). Fix likely needs an `entitlements` file with `keychain-access-groups`, OR a host-app target for unit tests, OR `kSecUseDataProtectionKeychain: true` on every Keychain call. Pre-existing latent issue — not a PR 14 regression. (added: 2026-06-06)
- [ ] **PR 20 — Tag + push v1.0.0 (user action)**: source is release-ready. Run `git tag v1.0.0 && git push origin v1.0.0` for the SDK; tag `relay-vercel-v1.0.0` and `relay-cloudflare-v1.0.0` against the same HEAD for hotfix freedom; submit to Swift Package Index via spi.yml. CHANGELOG, UserAgent.sdkVersion, README "Status" all updated to 1.0.0. (added: 2026-06-06)
- [ ] [After Initial Code Works Full Circle] - expand on the relay for the users, so we can report telemetry and other stats for them since we load in each application. If there is anything we can monitor that is beneficial, and provide the user with the ability to turn the monitor on and off, we could have a very powerful product on our hands. (added: 2026-06-06)

## Doing


## Done

- [x] **PR 1 — Bootstrap** _(2026-06-04)_: `Package.swift`, `LICENSE` (MIT), `README.md`, `CONTRIBUTING.md`, `SECURITY.md`, `CHANGELOG.md`, `.github/workflows/swift.yml`, `.spi.yml`, extended `.gitignore`, stub `Sources/GitTickets/PublicAPI/GitTickets.swift`, baseline tests. Verified `swift build` + `swift test` green on macOS (Swift 6.2.4), `xcodebuild` green for iOS Simulator.
- [x] **PR 2 — Public API skeleton** _(2026-06-04)_: full public surface laid down across `Configuration.swift`, `AuthMode.swift` (`RepoCoordinate`, `SharedSecret` with base64/hex inits, `DeviceFlowScope`), `Models.swift` (`Report`, `SubmittedIssue`, `ReportKind`, `ReportAttachment`), `GitTicketsError.swift` (12 cases + `CustomStringConvertible`), `Policies.swift` (`DiagnosticsPolicy`, `DiagnosticsRedactor` with email/IPv4/IPv6/bearer-token statics, `PrivacyPolicy`, `MyIssuesPolicy`), `Theme.swift` (`GitTicketsTheme`, `GitTicketsImageSource`, `GitTicketsButtonStyle`, SwiftUI `EnvironmentKey`), `Logger.swift`. 13/13 tests green on macOS, iOS Sim build clean.
- [x] **PR 3 — Bodybuilder** _(2026-06-04)_: internal `Sources/GitTickets/Bodybuilder/` — `CorrelationMarker` (HTML-comment UUID embed + whitespace-tolerant regex extractor, returns first marker / nil on absent or malformed UUID), `BodyTemplates` (per-`ReportKind` starter bodies + default labels), `IssueBodyBuilder` (assembles trimmed body + screenshot image + fenced diagnostics block + image/link attachments + correlation marker; marker always last so extractor never trips on earlier comments), `UploadedAttachment` struct. 22 new tests covering round-trip, unicode, whitespace-only inputs, multi-marker selection, malformed UUIDs. 35/35 green on macOS, iOS Sim clean.
- [x] **PR 4 — Storage** _(2026-06-04)_: internal `Sources/GitTickets/Storage/` — `Keychain` (Security-framework generic-password wrapper with `kSecAttrAccessibleAfterFirstUnlock`, update-then-add idempotent write), `DeviceIdentity` (configurable service/account, persists a generated UUID, supports `currentOrGenerate()` / `current()` / `reset()`), `SubmissionRecord` (SQLite row + `asSubmittedIssue` projection with `unreadReplyCount` derived), `SubmissionCache` (system `SQLite3` module, `@unchecked Sendable`, serial DispatchQueue, schema migration on open, indexed by `submitted_at DESC`, supports upsert / fetch / list / `markRepliesRead` / delete). 19 new tests across Keychain, DeviceIdentity, and SubmissionCache (round-trip, ordering, upsert-replace, NULL `latest_reply_at`, reopen persistence). 54/54 green on macOS, iOS Sim build clean.
- [x] **PR 5 — Diagnostics** _(2026-06-04)_: internal `Sources/GitTickets/Diagnostics/` — `RedactionPipeline` (sequential `NSRegularExpression` substitutions), `DeviceInfo` (utsname machine identifier + human-readable mapping table covering recent iPhones/iPads/Macs through M4 2026, fallback to raw on unknown, "Simulator (arch)" wrapper when running in simulator), `OSLogTailer` (`OSLogStore(scope: .currentProcessIdentifier)` filtered by subsystem predicate within lookback window; never throws — returns empty on any failure), `DiagnosticsBlob` (text + structured sections with custom `==`/`hash` because tuple arrays aren't `Hashable`), `DiagnosticsCollector` (assembles per-policy, runs redaction once at the end so what user sees == what's posted). Tightened IPv6 redactor regex to include `::` zero-compression forms after `2001:db8::1` slipped through. Test fix: copy `MemoryLayout.size(ofValue: info.machine)` to a local before `withUnsafePointer` to avoid Swift's exclusive-access trap on overlapping reads. 22 new tests (redaction edge cases including realistic blob, DeviceInfo mappings + unknown fallback, collector smoke tests + custom redactor). 76/76 green on macOS, iOS Sim build clean.
- [x] **PR 6 — Screenshot** _(2026-06-04)_: internal `Sources/GitTickets/Screenshot/` — `ScreenshotCapture` entry point with `ScreenshotCaptureError` (`.permissionRequired`, `.captureFailed(String)`, `.noActiveWindow`, `.encodingFailed` — Equatable), `+macOS.swift` (ScreenCaptureKit `SCScreenshotManager.captureImage` on macOS 14+, `OneShotStream` fallback for macOS 13 using `SCStreamOutput`, permission-error detection by string-matching the SCK error text, PNG via `NSBitmapImageRep`), `+iOS.swift` (`@MainActor`, `UIGraphicsImageRenderer.image` rendering the key window via `UIWindow.drawHierarchy`, key-window resolution via `UIApplication.connectedScenes`). Capture is initiated only on user tap — SDK never auto-captures. Permission failure returns recoverable `Result` so the form falls back to text-only submission. 2 error-equality tests; full capture verification deferred to the sample apps in PR 19. 78/78 green on macOS, iOS Sim build clean.
- [x] **PR 7 — Networking core** _(2026-06-04)_: internal `Sources/GitTickets/Networking/` — `UserAgent` (`GitTickets/<sdkVersion> (<platform>; <DeviceInfo>; <osVersion>) <appName>/<appVersion>`), `RateLimitBackoff` (parses delta-seconds + HTTP-date `Retry-After`, exponential backoff with cap, attempt-aware), `HTTPClient` (URLSession adapter, injects User-Agent without overriding caller-supplied value, bounded retries on transport errors + 5xx, EXPLICITLY does not retry 4xx including 429 — caller maps to `.rateLimited` via the `Retry-After` header, `HTTPResponse` envelope with case-insensitive header lookup). `URLProtocol`-based `MockURLProtocol` test harness covers retry-then-succeed, no-retry-on-4xx, no-retry-on-429-but-returns-Retry-After, header injection precedence, transport-error-retry-then-throw. 15 new tests. 93/93 green on macOS, iOS Sim build clean.
- [x] **PR 8 — Relay submitter** _(2026-06-04)_: internal `Sources/GitTickets/Auth/` — `IssueSubmitter` protocol (Sendable async submit), `Relay/RelayPayload` (Codable wire DTOs versioned with `schemaVersion: 1`: `RelayReportRequest`/`Response`, `RelayAttachmentResponse`, `MyIssuesRequest`/`Response`/`Item`, `RelayErrorEnvelope`; `RelayJSON.encoder/decoder` with `.sortedKeys` + `.withoutEscapingSlashes` + ISO8601), `Relay/RelaySignature` (HMAC-SHA256 over `"<timestamp>.<body>"`, returns `"sha256=<lowercase hex>"`, constant-time `verify`), `Relay/RelayClient` (signed POSTs, multipart attachment upload, status code → `GitTicketsError` mapping: 401→signatureMismatch, 413→attachmentTooLarge, 429→rateLimited(retryAfter:), 5xx→relayRejected), `Relay/RelaySubmitter` (orchestrates upload screenshot → upload attachments → assemble body via `IssueBodyBuilder` → POST `/report` → cache via `SubmissionCache` best-effort → return `SubmittedIssue`). Added `Report.diagnosticsBlob: String?` so the form pre-redacts once and the submitter never re-collects (critical "what user sees == what gets sent" invariant). HMAC cross-check test recomputes the canonical input via CryptoKit directly — this is the contract the Node/Cloudflare implementations must match. 16 new tests covering signature stability, validation, happy path (with header + cache assertions), diagnostics opt-out, attachment ordering, every error code mapping. 109/109 green on macOS, iOS Sim build clean.
- [x] **PR 9 — Vercel relay** _(2026-06-05)_: full `relay/vercel/` Node 20 TypeScript implementation matching the locked wire contract. Structure: `api/report.ts` + `api/attachment.ts` + `api/my-issues.ts` (`bodyParser: false` so raw bytes are signable); `api/_lib/{env, hmac, githubApp, payload (zod), rateLimit, idempotency, blob, multipart, handler, github, logger}.ts`. Plus `relay/shared/payload-schema.md` (language-agnostic spec — both templates implement this). Hardenings beyond plan: PKCS#1→PKCS#8 conversion guidance in env loader error message; multipart MIME whitelist mirroring the Swift side; rejects POST /report without `X-GitTickets-Idempotency-Key`; idempotency `conflict` 409 when same key arrives with different body hash; Upstash Redis optional (in-memory fallback for low-volume). HMAC vector LOCKED on both sides: `sha256=54f12328229a172be47ba5bd5383957265a2f482cfa072373e82783f4805b1c6` for `("1700000000", `{"hello":"world"}`, 32×0x37)`. 37/37 vitest green, `tsc --noEmit` clean. New CI workflow `.github/workflows/relay-vercel.yml`. Real deployment + GitHub round-trip verification deferred to user (needs GitHub App creation).
- [x] **PR 10 — Cloudflare Worker** _(2026-06-05)_: full `relay/cloudflare/` parity port. Workers runtime (Web Crypto, no `node:crypto`; jose works directly via Web Crypto); R2 binding for attachments; KV namespaces for rate limit + idempotency (with in-memory fallback). Layout mirrors Vercel: `src/worker.ts` entry with router, `src/handlers/{report,attachment,myIssues}.ts`, `src/lib/{env,hmac,githubApp,payload,rateLimit,idempotency,r2,multipart,github,responses,auth,logger}.ts`. Multipart parser ported from Node Buffer to pure `Uint8Array`. Routes accept both `/foo` and `/api/foo` so a single curl smoke recipe works against both Vercel and CF deployments. R2 URL builder uses an `.invalid` fallback host when `GITTICKETS_R2_PUBLIC_BASE_URL` is unset — broken images in your issues are LOUD, not silent. HMAC vector test locked to the same hex the Vercel + Swift tests assert. 32/32 vitest green, `tsc --noEmit` clean. New CI workflow `.github/workflows/relay-cloudflare.yml`. Real Workers deploy + GitHub round-trip verification deferred to user.
- [x] **PR 15 — My Issues** _(2026-06-06)_: Phase 2 surface — past-submissions list, issue detail with the live comment thread, manual refresh (pollInterval=0 default). Public `IssueComment` model. `IssueSubmitter.fetchComments(issueNumber:deviceID:)` default added; both submitters override. `RelaySubmitter.fetchComments` posts to `/comments` (new endpoint) and projects to `[IssueComment]`. `DeviceFlowSubmitter.fetchMyIssues` walks the cache's known submissionIDs and GETs `/repos/:owner/:name/issues/:n` per record (cheap with manual refresh — a few requests max), rebuilds the SubmissionRecord with fresh title/comments/updated_at, preserves local `readReplyCount`. `DeviceFlowSubmitter.fetchComments` GETs `/repos/:owner/:name/issues/:n/comments` and maps to `IssueComment`; 401 wipes the token and throws `.deviceFlowNotAuthorized` so the form re-prompts. Public static methods on `GitTickets` for adopters building custom UIs: `cachedSubmissions()`, `refreshMyIssues()`, `fetchComments(issueNumber:)`, `markRepliesRead(submissionID:count:)`. UI under `Sources/GitTickets/UI/SwiftUI/`: `GitTicketsMyIssuesView` (public — cache-first list with unread-count capsule badge, navigation-stack push to detail, ToolbarItem refresh, empty + unconfigured states), `IssueDetailView` (public — title header + Open-on-GitHub + comment thread, marks read on `.task` so the badge clears immediately, network load with idle/loading/loaded/failed states), `MarkdownCommentView` (public — renders the body via `AttributedString(markdown:)` with `interpretedSyntax: .inlineOnlyPreservingWhitespace` so a stray `# heading` line doesn't eat the rest, plain-text fallback on parse failure). Relay: Vercel `api/comments.ts` + Cloudflare `src/handlers/comments.ts` + router/vercel.json updates + shared `payload-schema.md` updated. New `listComments()` helper in both `_lib/github.ts` paging 5×100=500 max; 404 → 200 with empty comments (issue gone or invisible to installation — SDK renders "no replies yet" rather than a hard error). `CommentsRequestSchema` + `CURRENT_COMMENTS_SCHEMA_VERSION=1` in both zod payload modules. 6 new Swift tests (RelaySubmitter fetchComments round-trip + malformed-date drop; DeviceFlowSubmitter fetchMyIssues GitHub-GET cache update + no-token throw, fetchComments round-trip + 401-wipes-token). 209/209 Swift green, 37/37 Vercel + 32/32 Cloudflare green, iOS Sim clean.
- [x] **PR 17 — Documentation** _(2026-06-06)_: 8 docs landed under `docs/`: `getting-started.md` (auth-mode picker → install → configure → wire UI), `relay-deployment.md` (deep-link to template READMEs + common failure modes — 401-signature-mismatch with the openssl-hmac literal-vs-hex gotcha inline, 401-from-GitHub, PEM newline corruption), `device-flow.md` (when/when-not + GitHub OAuth App setup walk-through + user-facing UX flow + token revocation behavior + limitations), `theming.md` (every `GitTicketsTheme` field with default/effect table + `GitTicketsImageSource` cases), `diagnostics.md` (default policy + opt-outs + OSLog wiring + custom redactor pattern with over-match warning), `privacy.md` (manifest declarations table + what adopters must add to their own manifest + per-claim opt-outs), `threat-model.md` (what we protect against + what we don't + compliance posture), `architecture.md` (ASCII data flow diagram + two auth-mode dispatch summary + submission pipeline + locally-stored data table). Root `README.md` updated: stale `GitTicketsCommands()` (no-args) example replaced with the correct closure form + a sheet, `wiki/` links swapped for `docs/` links. Inline cross-references between docs all internal — no `.memory/` paths leaked into adopter-facing docs (older footgun filenames have spaces + em-dashes that don't URL-resolve cleanly). Relay README screenshots deferred — needs a clean GitHub App walkthrough that's user-action-dependent. 203/203 Swift green.
- [x] **PR 19 — Examples polish** _(2026-06-06)_: `Examples/` directory shipped with reference integration code for the three host shapes (no standalone `.xcodeproj` skeletons — the file format is fragile and the integration code itself is the value): `MacSampleApp/MacSampleApp.swift` (SwiftUI macOS App + `GitTicketsCommands` + Window scene hosting `GitTicketsView`, with a `ReportIssueCommands` wrapper that captures `@Environment(\.openWindow)` — same pattern Memophant uses), `iOSSampleApp/iOSSampleApp.swift` (SwiftUI iOS App + `.sheet(isPresented:)` presenting `GitTicketsView`), `AppKitSampleApp/AppKitSampleApp.swift` (pure AppKit `AppDelegate` + `GitTicketsMenuItemFactory.makeReportIssueItem()` + `ReportWindowController.shared`). `Examples/README.md` with a table mapping host shape → directory → wired surfaces, plus the three placeholders adopters must replace (relay URL, hex shared secret, repo coords). Cross-references `docs/getting-started.md` + `docs/relay-deployment.md` which land in PR 17.
- [x] **PR 18 — Privacy manifest** _(2026-06-06)_: `Sources/GitTickets/PrivacyInfo.xcprivacy` shipped as a target resource via `Package.swift`'s `.copy("PrivacyInfo.xcprivacy")`. Audit-driven contents — declared only what the SDK actually does, dropped the speculative `CrashData` / `PerformanceData` / `UserDefaults` claims from the draft (greps confirmed no usage). Declared: `NSPrivacyTracking=false`, no tracking domains, three collected data types (`OtherDiagnosticData` for the diagnostics blob, `DeviceID` for the per-install UUID from `DeviceIdentity`, `PhotosorVideos` for user-attached images) all `Linked=false / Tracking=false / Purpose=AppFunctionality`, one required-reasons API (`DiskSpace` reason `85F4.1` for `URL.resourceValues(forKeys: [.volumeAvailableCapacityForImportantUsageKey])` in `DiagnosticsCollector.freeDiskDescription`). Per-section comments document WHY each claim is there so a future audit doesn't have to re-grep. Adopter guidance for `docs/privacy.md` deferred to PR 17. 203/203 Swift green, iOS Sim build clean, 37/37 Vercel + 32/32 Cloudflare untouched.
- [x] **PR 16 — Theming polish** _(2026-06-06)_: Closes the two remaining unread theme values flagged in [[Audit Pass 2 — Post-Submit-Wiring Sweep]] and [[PR 12 Complete — SwiftUI Form]]. `GitTicketsView.header` now routes the icon through a new internal `HeaderImage` view that switches on `GitTicketsImageSource` (`.systemSymbol` → `Image(systemName:)`; `.named` → `Image(_:bundle:)` with `Bundle(identifier:)` lookup + main-bundle fallback; `.data` → `NSImage(data:)`/`UIImage(data:)` shim with system-symbol fallback on decode failure). Submit button refactored into `@ViewBuilder submitButton(configuration:)` with a switch on `GitTicketsTheme.submitButtonStyle` so all three button-style enum cases (`.bordered`, `.borderedProminent`, `.plain`) actually apply (each SwiftUI style is a distinct type, so each branch returns its own concrete View). Snapshot tests added (`Tests/GitTicketsTests/UI/SwiftUI/SnapshotTests.swift`, macOS-only) via the existing `swift-snapshot-testing` dep — 6 baselines: PrivacyBanner light/dark/private/override + GitTicketsView unconfigured light/dark. NSHostingController appearance set via `NSAppearance(named: .darkAqua)` because `.preferredColorScheme(.dark)` doesn't propagate to the host's `NSAppearance` in test-runner context — light/dark SHAs verified distinct. Baselines under `__Snapshots__/SnapshotTests/`. 203/203 Swift green, iOS Sim build clean, 37/37 Vercel + 32/32 Cloudflare untouched.
- [x] **PR 14 — UIKit container** _(2026-06-06)_: `Sources/GitTickets/UI/UIKit/GitTicketsViewController.swift` (iOS-only via `#if os(iOS)`). Subclasses `UIHostingController<GitTicketsView>` rather than re-implementing the child-VC embed — the hosting controller already does the safe-area / sizeThatFits / layout-pass bridging. Programmatic-only init (`init(coder:)` marked `@available(*, unavailable)`) because `rootView: GitTicketsView()` depends on the `GitTickets.configuration` singleton being set, which can't be expressed in IB. Default `title = "Report an Issue"` for navigation-bar presentation. The form's built-in Cancel button uses `@Environment(\.dismiss)` which UIKit wires to modal-dismiss or nav-pop automatically. 2 new iOS-only tests (init sets title; type identity holds) — they don't run during `swift test` (macOS) but run + pass under `xcodebuild test` on the iOS Sim scheme. Discovered + filed a separate pre-existing issue: 26 Keychain-using tests fail on iOS Sim XCTest with `errSecMissingEntitlement`, NOT a PR 14 regression. macOS 197/197 swift test green, iOS Sim build clean (191 tests run, 26 pre-existing Keychain entitlement failures, 2 new UIKit tests pass), 37/37 Vercel + 32/32 Cloudflare untouched.
- [x] **PR 13 — SwiftUI Commands + AppKit factory** _(2026-06-06)_: `Sources/GitTickets/UI/SwiftUI/GitTicketsCommands.swift` — public `Commands` group placed via `CommandGroup(after: .help)` with an injected action closure so the host decides what "Report an Issue\u{2026}" does (present sheet, open scene, call `ReportWindowController` — UI-agnostic). Optional keyboard shortcut (defaults to none — Help-menu items rarely need one and ⌘? is system-reserved). Cross-platform (`macOS 13 / iOS 16`). `Sources/GitTickets/UI/AppKit/` (macOS-only via `#if os(macOS)`) — `GitTicketsMenuItemFactory.makeReportIssueItem(title:keyEquivalent:keyEquivalentModifierMask:action:)` returns a fully-wired `NSMenuItem`; the `MenuActionTarget` trampoline gets retained via `representedObject` because `NSMenuItem.target` is weak and the trampoline would otherwise deallocate on return (test_targetIsRetainedAcrossScope locks the footgun). `ReportWindowController` — `public final class : NSWindowController` hosting `GitTicketsView` inside `NSHostingController`, `shared` singleton for menu-action callers, `windowFrameAutosaveName` = "GitTickets.ReportWindow" so position persists, `showWindow(_:)` replaces the content view controller on each open so the form starts blank (privacy: typed-but-abandoned body shouldn't leak between opens), recenters when the autosaved frame falls on a disconnected display. 12 new tests (4 commands, 4 menu factory incl. lifetime retention, 4 window controller incl. fresh-state replacement contract). 194/194 Swift green, 37/37 Vercel + 32/32 Cloudflare untouched, iOS Sim clean.
- [x] **PR 12 — SwiftUI form** _(2026-06-06)_: `Sources/GitTickets/UI/SwiftUI/` — `PrivacyBanner` (public, copy derived from `RepoVisibility` + `PrivacyPolicy` override), `DiagnosticsDisclosure` (public, expanded by default, `monospacedFont` + `cornerRadius` from theme), `ReportFormFields` (kind picker + title + body; `bodyFont` + `cornerRadius` from theme), `ScreenshotThumbnail` (cross-platform image preview via `NSImage`/`UIImage` shim + filename + filesize chip), `DeviceFlowSheet` (public — wraps `DeviceFlowCoordinator` + `TokenStore` + `ASWebAuthenticationSession` with `prefersEphemeralWebBrowserSession=true`, `verificationURIComplete`, cross-platform `ASWebAuthenticationPresentationContextProviding` for `NSApp.keyWindow` / `UIWindowScene` lookup), `GitTicketsView` (public — composes the lot, reads `Configuration` from `GitTickets.configuration` singleton with test-only injection init for previews, state machine idle/submitting/succeeded/failed, intercepts `.deviceFlowNotAuthorized` → presents `DeviceFlowSheet` → on success re-submits; attachment UI hidden when `auth == .deviceFlow`; success view's "Open on GitHub" uses cross-platform `openURL` env value). Theme `accentColor`/`titleFont`/`bodyFont`/`monospacedFont`/`cornerRadius` now wired — `headerImage` + `submitButtonStyle` still unread, deferred to PR 16. Snapshot tests punted to PR 16 too (layout still in flux from Memophant integration). 17 new tests (banner copy public/private/override/empty-override, submit-disabled reasons across 5 states, attachment-support gating per auth mode, mimeType table, ScreenshotThumbnail decoder happy + garbage path, init both-branches build). 182/182 Swift green, 37/37 Vercel + 32/32 Cloudflare untouched, iOS Sim clean.
- [x] **PR 11 — Device Flow submitter** _(2026-06-06)_: hand-rolled OAuth Device Flow under `Sources/GitTickets/Auth/DeviceFlow/`. `DeviceFlowPayloads` (Codable wire DTOs + form-url-encoder + JSON coders), `TokenStore` (Keychain wrapper, bundle-id-namespaced service like `DeviceIdentity` so two same-team apps don't share a token), `DeviceFlowCoordinator` (state machine: `requestAuthorization` + `pollForToken` handling `authorization_pending`, `slow_down` per RFC 8628 §3.5 with server-`interval` re-negotiation, `expired_token`, `access_denied`, unknown-error, malformed-response; wall-clock cutoff at `expiresAt` so an abandoned browser doesn't spin forever; injectable `sleep` + `clock` for fast tests), `DeviceFlowSubmitter` (`IssueSubmitter` conformer — validates title, rejects screenshots/attachments with `.attachmentNotSupportedInDeviceFlow`, reads token from `TokenStore`, builds body via shared `IssueBodyBuilder` with `screenshotURL=nil`, POSTs to `/repos/:owner/:name/issues` with `Bearer` + `X-GitHub-Api-Version: 2022-11-28`, surfaces missingLabels via shared `RelaySubmitter.missingLabels` so both submitters agree, caches identically to relay path). 401 from GitHub deletes the dead token and throws new `.deviceFlowNotAuthorized` so the form re-prompts. `GitTickets.resolveSubmitter` wired with the same shape-match check the audit pass demanded — `test_deviceFlowDispatchReachesSubmitter` locks it. 31 new tests (6 TokenStore, 12 Coordinator, 12 Submitter, 1 dispatch). 165/165 Swift green, 37/37 Vercel + 32/32 Cloudflare untouched and re-confirmed green, iOS Sim build clean.
- [x] **Swift 6 language mode migration** _(2026-06-23)_: `Package.swift` `swift-tools-version 5.9→6.0` + package-level `swiftLanguageModes: [.v6]` (lib + tests both in Swift 6 mode; `-swift-version 6` confirmed passed to swiftc). Zero runtime-behavior change — only annotations + one rename + build config. Fixes by type: **(A)** `RelaySubmitter` ISO8601 statics `iso8601`/`iso8601NoFractional` → `nonisolated(unsafe)` (immutable-after-init, parse-only/inbound — outbound wire serialization via `RelayJSON.encoder` untouched; kept `ISO8601DateFormatter` not `FormatStyle` to preserve byte-identical parsing). **(B)** `GitTicketsMenuItemFactory` enum + `MenuActionTarget` trampoline → `@MainActor` (genuine AppKit UI construction; enforces the main-thread requirement that already existed — no work moved between threads). **(D)** 6 UI test classes → `@MainActor` (`GitTicketsMenuItemFactoryTests`, `ReportWindowControllerTests`, `GitTicketsCommandsTests`, `GitTicketsMyIssuesCommandsTests`, `GitTicketsViewTests`, `SnapshotTests`). **(E)** `DeviceInfo` `String(validatingUTF8:)`→`String(validatingCString:)` (pre-existing deprecation surfaced by clean rebuild; pure rename, identical semantics). NOTE: the `AttributeScopes.SwiftUIAttributes` non-Sendable keypath warnings seen under Swift-5 `-strict-concurrency=complete` do NOT fire in true v6 mode on Xcode 26.5 — `DiagnosticsRendering` left untouched. Verified: macOS lib+tests clean `swift build`/`--build-tests` (0 warn/0 err); iOS source incl. all `#if os(iOS)` paths clean via SwiftPM cross-compile to `iphonesimulator26.5` (0/0); `swift test` 208/210 — the 2 `SnapshotTests` form failures are PRE-EXISTING baseline drift (fail identically on pristine `main`/Swift 5; NOT migration-caused). Known env quirk: standalone `xcodebuild -scheme GitTickets` for iOS hits a beta-Xcode-26.5 "Supported platforms is empty" scheme bug — does not affect `swift build`/`swift test` or dependency consumption (Memophant). Snapshot baseline re-record left as a separate decision.

## Archived

