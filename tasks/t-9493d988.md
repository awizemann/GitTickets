---
id: t-9493d988
title: v2.1.0 P5: relay docs cross-ref, CHANGELOG, tag 2.1.0
status: done
added: 2026-07-26
priority: high
---

## Description

Final phase, owned by the orchestrating session. Covers ShabuBox asks #5 (already-built label detection — docs only) and #6 (tagging hygiene), plus version metadata, CHANGELOG, integration audit and the 2.1.0 tag.

## Plan

ASK #5 IS ALREADY IMPLEMENTED IN v2.0.0 — do NOT build it. Verified in code:
- RelayPayload.swift:58 parses `appliedLabels`.
- RelaySubmitter.swift:105 compares requested vs applied, case-insensitively (GitHub normalizes label case).
- RelaySubmitter.swift:106-112 emits logger?.log(level: .warning) naming the dropped labels.
- Models.swift:177 exposes public `SubmittedIssue.missingLabels`, whose doc already describes the exact scenario (App lacks push permission -> GitHub silently drops labels), with nil meaning "relay didn't report" and an explicit warning to treat nil as unknown rather than all-applied.
- DeviceFlowSubmitter.swift:97-114 does the same on the OAuth path.
All three mechanisms the request asked for (log line, warning, non-fatal response field) exist today.

THE ACTUAL GAP IS DISCOVERABILITY: missingLabels is documented in docs/device-flow.md and wiki/Patterns-and-Gotchas.md — but NOT in the relay docs, and ShabuBox is a relay adopter. Fix: cross-reference it from docs/relay-deployment.md (troubleshooting) and docs/diagnostics.md if appropriate. Tell ShabuBox they can delete their manual runbook step now: set configuration.logger and inspect SubmittedIssue.missingLabels.

REMAINING WORK:
1. Merge P1-P4 worktrees; verify the COMBINED tree (no agent sees the others). Expect clean merges given disjoint ownership; investigate any conflict as scope creep. Note P4 is sequenced after P1 on Policies.swift.
2. UserAgent.sdkVersion 2.0.0 -> 2.1.0 plus its doc-comment example.
3. CHANGELOG [2.1.0]: additive minor. Lead with the ONE behavior change — .absolutePath joining the default redactor array, with the opt-out shown, since adopters on defaults will see paths start disappearing from diagnostics. Then the additive items. Say explicitly that no public API was removed or changed in signature. Keep asserting only measured toolchain facts; do NOT invent a minimum Xcode version (this is the discipline v2.0.0 established after v1.1.0 shipped a false claim).
4. Confirm the non-goals held: GitTickets.configure, .relay(url:sharedSecret:), GitTicketsCommands, DiagnosticsPolicy(redactors:) and PrivacyPolicy(bannerText:requireExplicitConsent:) all still compile untouched. Verify by BUILDING a consumer package against those exact call sites, not by reading the diff. Also confirm no AuthMode case was added (source-breaking for exhaustive switches) and the platform floor is UNCHANGED at macOS 14 / iOS 18.
5. Full verification: swift build 0 warnings, swift test green, generic iOS Simulator build, and a pristine-clone CI-equivalent run (TEST_RUNNER_CI=true xcodebuild test).
6. Independent adversarial fresh-eyes audit of the cumulative diff, separate from the per-phase self-audits.
7. ASK #6 — TAGGING HYGIENE, and this is the point of the whole ticket: push main, WAIT for CI to go green, and only THEN tag v2.1.0 and push the tag. That ordering is what v2.0.0 established and what v1.1.0 got wrong (announced but never tagged, so `from: "1.1.0"` failed SPM resolution outright and forced ShabuBox to pin 1.0.0).
8. Prove the tag: resolve `from: "2.1.0"` from the PUBLIC remote in a throwaway consumer package and build it. v1.1.0's failure would have been caught instantly by this one check.
9. Report to Alan what ShabuBox must do on their side: repin to 2.1.0 (a minor, so an existing upToNextMajor-from-2.0.0 pin WILL auto-resolve, unlike the 1.x -> 2.0.0 jump), enable the two new opt-in options, and drop their custom path redactor in favor of the built-in.

## Artifacts

v2.1.0 shipped 2026-07-26.

- `main` at `0041739` (merge of `release/v2.1.0`), pushed to origin.
- Release-prep commit `5398155` (version bump + CHANGELOG/README/4 docs).
- Tag `v2.1.0` = `528ec4a`, dereferences to `0041739`, pushed.

Ordering held (ask #6): merge -> push -> CI green -> tag -> prove.

CI run 30214507209 on `0041739`, both jobs green:
- macOS 1m36s — 290 executed / 6 skipped / 0 failures
- iOS 2m54s — 278 executed / 30 skipped / 0 failures
Only annotation is GitHub's Node 20 deprecation on actions/cache@v4 and
actions/checkout@v4 — infra noise, present on the 2.0.0 runs too.

Tag proof (step 8) — throwaway consumer, cold SPM cache, resolved from the
PUBLIC remote: `from: "2.1.0"` -> version 2.1.0 @ 004173971607eae2. Built
AND ran (exit 0). It asserted, against the tagged artifact rather than the
working tree: v2.0.0-era call sites still compile (GitTickets.configure,
.relay(url:sharedSecret:), DiagnosticsPolicy(redactors:) explicit array,
PrivacyPolicy(bannerText:requireExplicitConsent:), exhaustive AuthMode
switch); PrivacyPolicy.default.attachmentNames still .filename;
.absolutePath present in defaults AND ordered before .email; the four
degenerate SharedSecret inputs rejected while 0xdeadbeef / 3q2+7w== still
decode; SharedSecret(infoPlistKey:encoding:) returns nil on absent key.

Correction to prior sessions' record: v1.1.0 IS tagged (tag object dated
2026-07-25, pointing at 51b13d9 from 2026-07-12) and is on origin. The
"announced but never tagged" framing is stale — it was backfilled. The
push-before-tag discipline stands on its own merits regardless.

