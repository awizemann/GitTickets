---
id: t-822c22e2
title: Release GitTickets v1.1.0 (Swift 6) — validate in Memophant, roll out to GitHub
status: done
added: 2026-06-23
priority: high
---

## Description

Ship the Swift 6 language-mode migration (commit 22ddab9, unreleased, 8 commits ahead of origin/main) as GitTickets v1.1.0. Validate it as a dependency inside Memophant's own Swift 6 build BEFORE pushing to GitHub, then roll out.

Key facts (grounded):
- v1.0.0 already shipped (tags v1.0.0 / relay-vercel-v1.0.0 / relay-cloudflare-v1.0.0 on github.com/awizemann/GitTickets).
- Memophant consumes GitTickets REMOTELY: XCRemoteSwiftPackageReference → github.com/awizemann/GitTickets, requirement upToNextMajorVersion minimumVersion 1.0.0 → accepts [1.0.0, 2.0.0). So v1.1.0 auto-resolves with NO pbxproj edit.
- Test-before-push strategy: temporarily override Memophant's remote dep with a LOCAL package ref to /Users/awizemann/Developer/GitTickets, validate, then cut tag + switch back.
- Version chosen: v1.1.0 (minor) — Swift 6.0 toolchain floor + @MainActor on GitTicketsMenuItemFactory; zero runtime-behavior change.
- We never push to GitHub until Alan explicitly says go (Phase 4).

## Plan

Phase 1 — Release prep in GitTickets (LOCAL, no push):
- UserAgent.sdkVersion "1.0.0" → "1.1.0".
- CHANGELOG: add [1.1.0] — <date>: Swift 6 language mode (tools-version 6.0 + swiftLanguageModes [.v6]); min toolchain Swift 6.0; GitTicketsMenuItemFactory now @MainActor; nonisolated(unsafe) ISO8601 statics; validatingCString rename. Zero runtime-behavior change.
- README "Status" → v1.1.0 line.
- swift build + swift test green (re-confirm with version bump). Commit locally.

Phase 2 — Wire Memophant to local GitTickets (Memophant repo, temp):
- Add local Swift package override pointing at ../GitTickets (Xcode "Add Local Package" overrides the same-identity remote ref). No GitHub needed.
- Confirm Memophant's own language mode (is it on Swift 6 yet?). The point is to exercise GitTickets inside Memophant's Swift 6 build.

Phase 3 — Test (the gate):
- Build Memophant (macOS) against local GitTickets.
- Run Memophant test suite.
- Dogfood: Help → Report an Issue → submit → GitHub issue created (round-trip). Prefer detached build to dogfood without disrupting Alan's running copy.
- Adversarial check: any Swift 6 friction at the dependency boundary; @MainActor menu factory still usable from Memophant's call site.

Phase 4 — Release + GitHub rollout (REQUIRES ALAN'S EXPLICIT GO):
- Review the 8 unpushed commits before pushing main.
- Tag v1.1.0 on the release commit; push main + tag to origin.
- Verify GitHub CI (Build & Test, macos-14/latest-stable Xcode) green on the pushed ref. Note pre-existing fragilities not caused by us: gitignored snapshot baselines (record-fresh on CI), iOS Sim keychain entitlement failures.
- Remove Memophant's local override; restore remote ref; resolve to v1.1.0; final build/test parity check.
- Commit Memophant's Package.resolved bump (Memophant repo, its own push rules).

Phase 5 — Close out:
- GitTickets memory: release note for v1.1.0; FIX the stale "Memophant Integration" note (it says local ../GitTickets path; Memophant now uses the remote tag).
- Mark tasks done.

OUT OF SCOPE (note, don't bundle): re-tagging relay templates (relay-*-v1.x) — they version independently and the relay is already deployed (tickets.memophant.co). SPI/Swift Package Index refresh — optional follow-up.

## Artifacts

CLOSED 2026-07-26 — superseded by the v2.0.0 release. Keeping this ticket open would misrepresent the state.

WHAT ACTUALLY HAPPENED vs this ticket's plan: Phase 1 (release prep) completed in June as recorded. Phases 2-3 (validate inside Memophant against a local package override BEFORE tagging) were NEVER RUN — the ticket was paused at Phase 2 and the code was later pushed to origin/main without that gate. Phase 4's tag step was also never done, which is how v1.1.0 came to be announced in the README and CHANGELOG while no v1.1.0 tag existed on the remote. A separate integration session hit exactly that: resolution failed, and it was pinned to v1.0.0.

RESOLUTION SEQUENCE (2026-07-25/26):
1. Confirmed by inspection that no v1.1.0 tag existed locally or on origin, while the v1.1.0 CODE was already on origin/main. Missing tag only.
2. Tagged and pushed v1.1.0 (annotated -> 51b13d9) on Alan's explicit go, after verifying build clean + 210/210 on Xcode 26.6.
3. Checking CI immediately afterward revealed v1.1.0's documented toolchain floor was FALSE: it does not build on Xcode 16.2 / Swift 6.0. The tag annotation had frozen that unverified claim. See the decisions note on the real toolchain floor.
4. That triggered the v2.0.0 work: platform floor to macOS 14 / iOS 18, deletion of the offending fallback, CI made meaningful, and all compatibility claims restated against measured evidence. Tracked in t-bd61de6c / t-ebc833b6 / t-59ed0d9c / t-ced6a69a / t-2b805ee2, all done.

v1.1.0 REMAINS PUBLISHED AND REMAINS BROKEN on Xcode 16.x. Deliberate: the tag was not moved or deleted, because moving a published tag is worse than an inaccurate annotation, and a consumer may already have resolved it. The correction lives in the CHANGELOG's [1.1.0] entry instead, with the original text preserved beneath it.

THE PROCESS LESSON, which is the durable part: this ticket's own plan had the right gate — "validate as a dependency BEFORE pushing to GitHub" — and skipping it is precisely what produced a published tag with a false claim. v2.0.0 restored the gate in a cheaper form: push the branch, let CI prove the claim, then tag. CI went green (its first ever) before the v2.0.0 tag was cut, so the tag annotation contains measured facts rather than intentions.

OUT OF SCOPE AND STILL OPEN, as this ticket originally noted: relay template re-tagging (relay-*-v1.x version independently and are unchanged) and any Swift Package Index refresh. Also still open: Memophant's own pin needs bumping deliberately, since upToNextMajorVersion from 1.x resolves [1.0.0, 2.0.0) and will NOT pick up 2.0.0.

