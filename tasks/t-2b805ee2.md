---
id: t-2b805ee2
title: v2.0.0 P5: integration audit, fresh-eyes review, tag 2.0.0
status: done
added: 2026-07-25
priority: high
---

## Description

Phase 5 of v2.0.0, owned by the orchestrating session (not a sub-agent). Merge P1-P3 worktrees onto release/v2.0.0, verify the combined result, audit every phase against its plan, run an independent adversarial fresh-eyes pass, then tag v2.0.0 on Alan's explicit go. Alan will exercise the tagged version in a real app afterward.

## Plan

1. Merge the three worktree branches onto release/v2.0.0. Expect clean merges (file ownership was disjoint by construction); investigate any conflict as a sign an agent exceeded its scope.
2. Verify the COMBINED tree, which no individual agent saw: swift build 0 warnings, swift test 210/210, xcodebuild generic iOS Simulator BUILD SUCCEEDED. A per-phase green does not imply a combined green.
3. Audit each phase against its task plan — including what each agent claimed it verified versus what it actually ran. Treat sub-agent self-reports as claims to check, not conclusions.
4. Specifically re-check the risk items: no load-bearing @available deleted; no error path changed in the screenshot rewrite; CI not made green-by-avoidance.
5. Then P4 (docs/version/CHANGELOG) sequentially in the main tree.
6. Independent adversarial fresh-eyes pass over the full cumulative diff — separate from the per-phase self-audits, which are not a substitute.
7. Merge to main, tag v2.0.0 annotated, push ONLY on Alan's explicit go. The tag annotation must claim only what was actually verified — the v1.1.0 tag overstated compatibility and that is the mistake this release exists to correct.
8. HAND-OFF TO ALAN: state plainly that macOS screenshot capture is compile-verified only (no headless TCC/display access), so it is the first thing to exercise in his app. Also note that a 2.0.0 pin does not auto-resolve for consumers on upToNextMajorVersion from 1.x.
9. Close out: update memory (the toolchain-floor note, the 1.1.0 release note), mark P1-P5 done.

## Artifacts

DONE 2026-07-26. v2.0.0 shipped: main at 0583bf5 (merge) then a491180 (CI-result docs fix); annotated tag v2.0.0 -> a491180 pushed to origin.

CI IS GREEN — run 30203729406, the FIRST green Build & Test in this repository's history (every prior run back to 2026-06-04 was red). Both jobs succeeded:
- macOS: 210 executed / 6 skipped / 0 failures
- iOS: 198 executed / 30 skipped / 0 failures
- iOS destination resolved to a REAL iOS 18.6 simulator (grep confirmed "OS:18.6" in the job log), so the floor is tested AT the floor, not merely above it.
These numbers match P3's local predictions exactly (204+6 macOS, 198+30 iOS), which independently corroborates its analysis.

BOTH OPEN RISKS FROM P3 ARE NOW CLOSED BY EVIDENCE, not argument: the OS=18.6 destination resolves on macos-15, and Xcode 26.3 exists there and can drive an iOS 18.6 runtime. Neither was checkable locally (no iOS 18 runtime on the dev machine, local Xcode is 26.6).

SEQUENCING DECISION THAT PAID OFF: pushed main FIRST and waited for CI before tagging, rather than tagging immediately. That converted the toolchain claim from "configured but not yet executed" into a measured fact before it was frozen into a tag. Had CI failed on the destination or the Xcode pin, it would have failed on an untagged commit — cheap to fix. This is the direct lesson from v1.1.0, whose tag annotation froze an unverified claim.

Consequently a491180 was needed: the docs said CI "has not run yet", which was true when written and stale by the time of tagging. Fixed before the tag so the released text matches reality.

FINAL AUDIT (orchestrator, independent of all agent self-reports):
- Pristine-clone RC verification: swift build --build-tests 0 warnings; TEST_RUNNER_CI=true xcodebuild test -> TEST SUCCEEDED 210/6 skipped; generic iOS Simulator BUILD SUCCEEDED; 0 stray files after a full cycle.
- Public API: zero public/open declarations added or removed across 51b13d9..HEAD. The major bump is purely the platform floor.
- Mutation-safety: P3's blocked mutation of RedactionPipeline verified reverted (blob byte-identical to base, zero Sources/ files in its commit). Completed the mutation test it could not: an unguarded clean checkout yields TEST FAILED, proving red propagates and CI is not green-by-construction.
- Gap check no phase owned: Examples/ is 3 standalone .swift files + README, no build system, no stale references. Only one Package.swift in the repo. Package.resolved untracked (correct for a library).
- END-TO-END REMOTE PROOF: a throwaway adopter package with `from: "2.0.0"` against the PUBLIC repo resolves the tag, builds, and runs — printed "resolved + consumed v2.0.0 from the remote tag: awizemann MyApp". This is the check that was never done for v1.1.0 and would have caught its failure immediately.

STILL OUTSTANDING, carried to hand-off:
1. macOS screenshot capture is COMPILE-VERIFIED ONLY. Real capture needs Screen Recording (TCC) permission and a display; unavailable headlessly. Untested at runtime: success path (SCScreenshotManager -> CGImage -> PNG), permission-denied, no-display. Alan exercises this in his own app — it is the single highest-value manual check.
2. macOS-only SwiftUI rendering now has ZERO CI coverage (snapshot suite is local-only by design). Run `swift test --filter SnapshotTests` locally before future releases.
3. `@preconcurrency import ScreenCaptureKit` is retained but provably unnecessary on Swift 6.3.3 — kept as a hedge when CI was still Swift 6.0. Now that CI is Xcode 26.3, it is a candidate for removal. Cosmetic; do not churn.
4. Public type is named `Configuration`, unnamespaced. A rename is breaking, so a major version is the cheapest moment — 2.0.0 has now passed. Standalone decision for Alan.

